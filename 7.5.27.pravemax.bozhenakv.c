#include <stdio.h>

unsigned int vyber_prave_maxima(int *in, unsigned int l, int *out) {
	int max = in[l-1];
	unsigned int count = 0;
	out[count]=max;
	count++; 
}

int main(){
	int max = in[l-1];
	unsigned int count = 0;
	out[count]=max;
	count++;
	
}